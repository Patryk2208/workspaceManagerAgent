import logging

from model.config import config

logger = logging.getLogger(__name__)

def run_agent():
    """
    main loop of the agent, get state updates, runs them through the model, posts verified commands back to the server.
    """
    pass

async def workspace_refinement_step():
    pass